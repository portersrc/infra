name: Porter GHA Testing
run-name: ${{ github.actor }} is doing the porter GHA testing
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Start"
      - run: echo "Issuing ls"
      - run: ls
      - run: echo "Issuing ls --version"
      - run: ls --version
      - run: echo "Issuing docker --version"
      - run: docker --version
      - run: echo "Issuing make hack"
      - run: make hack
      - run: echo "Issuing pushd container-images && make hack"
      - run: pushd container-images && make hack
      - run: echo "End"
